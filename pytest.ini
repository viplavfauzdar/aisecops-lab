[pytest]
markers =
    integration: requires running API + dependencies (docker compose)
    llm: requires an LLM provider (ollama/openai/anthropic)

addopts = -ra

# Suppress unknown mark warnings inside Docker where pytest.ini
# may not be discovered depending on working directory.
filterwarnings =
    ignore::pytest.PytestUnknownMarkWarning